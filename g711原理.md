# G.711 音频编码原理（通俗版）

## 一、G.711 是什么？

G.711 是国际电信联盟（ITU-T）制定的一种**语音压缩标准**，用于电话系统。

- 采样率：8000 次/秒（每秒采集 8000 个声音样本）
- 压缩前：每个样本 16 位（2 字节）→ 128 kbps
- 压缩后：每个样本 8 位（1 字节）→ 64 kbps
- **压缩比：2:1，数据量减半，但听感几乎无损**

G.711 有两种变体：

| 变体 | 使用地区 | 折线近似 | 输入位数 |
|------|---------|---------|---------|
| G.711A（A-law） | 欧洲、中国 | 13 折线 | 13 位 |
| G.711U（μ-law） | 北美、日本 | 15 折线 | 14 位 |

---

## 二、核心思想：人耳的特点

人耳对声音的感知是**对数性**的：

- **小声音**变化一点点，人耳就能察觉 → 需要精细记录
- **大声音**变化很多，人耳才感觉出来 → 不需要那么精细

G.711 就是利用这个特点，**不均匀地**分配精度：

```
声音强度：  小 ──────────────────────────────→ 大
精度分配：  高（精细）───────────────→ 低（粗糙）
```

打个比方：用尺子量东西，短距离刻度密（每毫米一个），长距离刻度稀（每厘米一个）。总刻度数减少了，但日常使用完全够用。

---

## 三、怎么做到的？——分段折线法

G.711 把声音强度分成 **8 个区间（段落）**，每个区间用不同的精度：

```
段落 0：最小信号，台阶=1（μ-law）或 2（A-law），最精细
段落 1：较小信号，台阶=2
段落 2：小信号，台阶=4
段落 3：中等信号，台阶=8
段落 4：较大信号，台阶=16
段落 5：大信号，台阶=32
段落 6：很大信号，台阶=64
段落 7：最大信号，台阶=128，最粗糙
```

每段台阶翻倍，就像楼梯越来越陡。

### 为什么叫"折线"？

如果把每个段落看作一条斜线段，8 段连起来就是一条**折线**，用来近似对数曲线：

- A-law：正负各 8 段，但第 0、1 段斜率相同，合并后共 **13 段折线**
- μ-law：正负各 8 段，各段斜率都不同，共 **15 段折线**

---

## 四、编码结构（8 位 = 1 字节）

```
┌──────┬────────┬────────┐
│ 符号 │ 段落码 │ 段内码 │
│ 1 位 │  3 位  │  4 位  │
└──────┴────────┴────────┘
```

- **符号位**：正还是负
- **段落码**：信号落在哪个区间（0~7）
- **段内码**：区间内的具体位置（0~15）

### 编码步骤

```
原始 16 位 PCM
    │
    ▼
取符号位，取绝对值
    │
    ▼
找最高有效位位置 → 确定段落码（哪一段）
    │
    ▼
取有效位 → 确定段内码（段内位置）
    │
    ▼
组合：符号位 + 段落码 + 段内码 = 8 位编码
    │
    ▼
A-law：偶数位取反（XOR 01010101）
μ-law：全部取反
```

---

## 五、A-law vs μ-law 对比

| 特性 | A-law (G.711A) | μ-law (G.711U) |
|------|----------------|----------------|
| 使用地区 | 欧洲、中国 | 北美、日本 |
| 压缩参数 | A = 87.6 | μ = 255 |
| 折线数 | 13 段 | 15 段 |
| 第 0 段台阶 | 2 | 1 |
| 小信号精度 | 稍低 | 更高 |
| 动态范围 | 72 dB | 78 dB |
| 空闲噪声 | 较低 | 较高 |

**简单说：μ-law 小信号精度更高，A-law 空闲时噪声更低。实际听感差异很小。**

---

## 六、为什么说"等效 13 位 / 14 位"？

这句话的意思是：G.711 用 8 位存储，但在小信号区域的精度相当于 13~14 位的线性编码。

| 信号大小 | 等效精度 |
|---------|---------|
| 小信号（段落 0） | ≈ 13~14 bit |
| 中等信号（段落 3~4） | ≈ 10~11 bit |
| 大信号（段落 7） | ≈ 8 bit |

整体动态范围相当于 13 位（A-law）或 14 位（μ-law）的线性 PCM。

---

## 七、应用场景

- **传统电话**（PSTN）：数字电话的标准编码
- **VoIP 通话**：RTP 载荷类型 0（μ-law）/ 8（A-law）
- **视频会议**：H.323、SIP 协议中的默认音频编码

---

## 八、一句话总结

> G.711 用 8 位数据实现了约 13~14 位的动态范围。核心原理是**对数压缩**——小信号精细记录，大信号粗糙记录，符合人耳的听觉特性。A-law 用 13 折线近似，μ-law 用 15 折线近似，两者通过简单的查表和位操作即可完成编解码。
